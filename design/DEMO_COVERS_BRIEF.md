# Demo 封面 6 張 — Voxel 版

> 先讀 `VOXEL_STYLE_GUIDE.md`。每個 prompt = §2 共用前綴 + 下面場景。
> 存檔：`assets/img/src/voxel/demos/<slug>.png`，1536×1024。目的：讓人想點進去看，所以場景要「有東西在發生」，
> 但仍是同一套底座、視角、色盤；每張可以多用一點淺木色與鼠尾草當配角。

| slug | Demo | 接在前綴後的場景 |
|---|---|---|
| `bakery` | 手作烘焙預購頁 | a tiny voxel bakery counter with a pale-blue striped awning, four round golden-brown voxel loaves on the counter, a small paper bag, and a little voxel calendar block on the wall with one brand-blue cube marking a date (the tasting event); warm and inviting |
| `esg` | 移工 DEI 認證平台 | five small faceless voxel figures in different muted shirt colours standing in a half circle around a tall white voxel certificate card with a brand-blue ribbon block and a small QR-like block pattern (just cubes, no text); behind them a small voxel screen showing simple bar blocks |
| `ai-service` | 電商 AI 客服 | a large voxel chat window standing upright like a signboard, with three pale-blue and brand-blue speech-bubble blocks inside; a small round voxel cat sits on top of the window; a tiny package box and a delivery-truck block at the foot |
| `order` | 掃碼點餐系統 | a small voxel restaurant table with a standing card showing a QR-like block pattern (cubes only), a voxel phone lying on the table with menu blocks on screen, and on the back edge of the tile a white voxel kitchen board with three ticket cards pinned in a row |
| `roulette` | 推薦幸運輪盤 | a voxel prize wheel standing on a small stand, segments in pale blue, white and brand blue, a tiny pointer on top, a small round voxel lucky-cat figure beside it with one paw raised, and a few brass-coloured coin cubes on the tile |
| `streamer_ai` | 直播主 AI 小助手 | a voxel streaming desk with a ring light on a stand, a small microphone, a laptop, and a tall voxel phone showing chat-bubble blocks; three small brand-blue heart-shaped cube clusters floating above the desk |

## 驗收
除 `VOXEL_STYLE_GUIDE.md` §4：六張並排＋主站 6 張封面並排，要像同一套；沒有文字、沒有五官。
